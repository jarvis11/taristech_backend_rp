<%= form_for([@content, @adstatistic]) do |f| %>
  <% if @adstatistic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adstatistic.errors.count, "error") %> prohibited this adstatistic from being saved:</h2>

      <ul>
      <% @adstatistic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
